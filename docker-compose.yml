services:
  mongodb:
    image: mongo:latest
    container_name: vedai-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:latest
    container_name: vedai-redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
